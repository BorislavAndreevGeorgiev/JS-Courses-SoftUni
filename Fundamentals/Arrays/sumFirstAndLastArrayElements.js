function sumFirstAndLastArrayElements (n){

    let firstNum = n[0];
    let lastNum = n[n.length -1]
    let result = firstNum + lastNum
    console.log(result);

}

sumFirstAndLastArrayElements([20, 30, 40])